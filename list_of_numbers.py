word_list = ["apple", "salad", "books", "nairobi"]

odd_length_words = [word for word in word_list if len(word) % 2 != 0]

print("word list one:", word_list)
print("odd length word list:", odd_length_words)